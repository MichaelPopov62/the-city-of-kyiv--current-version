# Якщо рядок VITE_API_URL закоментовано/відсутній — у `npm run dev` форма йде через проксі Vite на :9000 (без CORS).
# Розкоментуйте лише для прямого запиту на Express:
# VITE_API_URL=http://localhost:9000
